# Backend API URL — required when deploying frontend to a different host (e.g. Render static site)
# Leave empty to use Vite's dev proxy (local development)
VITE_API_BASE_URL=https://ai-dev-autonomous-ci-cd-healing-agent.onrender.com
